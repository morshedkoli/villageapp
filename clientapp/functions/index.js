// Push notifications are now sent directly from the Flutter app via the
// OneSignal REST API (onesignal_api_service.dart).
// This Cloud Function is no longer needed and can be safely removed.
// Keeping it here only for reference.
//
// To remove: delete this file and run `firebase deploy --only functions`

